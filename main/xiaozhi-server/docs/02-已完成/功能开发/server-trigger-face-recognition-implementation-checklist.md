# 服务器触发人脸识别 - 任务检查清单

## 📋 任务总览

| 阶段                 | 任务数 | 预计时间 | 实际时间 | 状态          |
| -------------------- | ------ | -------- | -------- | ------------- |
| 阶段一：拍照机制     | 3      | 9h       | 3h       | ✅ 已完成     |
| 阶段二：人脸识别流程 | 2      | 5h       | 2h       | ✅ 已完成     |
| 阶段三：配置与开关   | 1      | 2h       | 0.5h     | ✅ 已完成     |
| **总计**             | **6**  | **16h**  | **5.5h** | **✅ 已完成** |

---

## 🎯 阶段一：完善拍照请求-响应机制

### ✅ 任务 1.1：实现基于MCP的拍照同步等待机制

**优先级**：P0（核心基础）  
**预计时间**：6小时  
**实际时间**：2小时  
**状态**：✅ 已完成  
**完成日期**：2024-02-14  
**依赖**：无

**修改文件**：

- [x] `main/xiaozhi-server/core/providers/doorlock/esp32_camera.py`

**实现内容**：

- [x] 添加 `pending_captures` 字典管理待处理请求
- [x] 实现 `capture_image()` 方法：
  - [x] 检查MCP客户端状态
  - [x] 检查 `capture_image` 工具可用性
  - [x] 创建 `asyncio.Future` 对象
  - [x] 注册图片上传回调到 `ImageUploadHandler`
  - [x] 调用 `call_mcp_tool()` 发送拍照请求
  - [x] 使用 `asyncio.wait_for()` 等待照片上传
  - [x] 实现超时处理（默认10秒）
  - [x] 清理资源（Future和回调）
- [x] 添加完整的日志记录
- [x] 添加异常处理和错误日志

**验收标准**：

- [x] 能成功调用ESP32的 `capture_image` 工具
- [x] 能等待并接收HTTP上传的照片
- [x] 超时机制正常工作（10秒）
- [x] 资源正确清理，无内存泄漏
- [x] 日志完整清晰

**测试**：

- [ ] 单元测试：MCP工具调用
- [ ] 单元测试：Future超时处理
- [ ] 单元测试：回调清理
- [ ] 集成测试：完整拍照流程

---

### ✅ 任务 1.2：HTTP服务器单例模式

**优先级**：P0（核心基础）  
**预计时间**：1小时  
**实际时间**：0.5小时  
**状态**：✅ 已完成  
**完成日期**：2024-02-14  
**依赖**：无

**修改文件**：

- [x] `main/xiaozhi-server/core/http_server.py`

**实现内容**：

- [x] 添加类变量 `_instance = None`
- [x] 在 `__init__()` 中保存单例：`SimpleHttpServer._instance = self`
- [x] 添加类方法 `get_instance()` 返回单例
- [x] 添加日志记录单例初始化

**验收标准**：

- [x] `SimpleHttpServer.get_instance()` 能返回实例
- [x] `ESP32CameraService` 能访问 `image_upload_handler`
- [x] 单例在服务器启动时正确初始化

**测试**：

- [ ] 单元测试：单例模式
- [ ] 集成测试：从其他模块访问单例

---

### ✅ 任务 1.3：修复 `_capture_visitor_photo` 方法

**优先级**：P0（核心功能）  
**预计时间**：2小时  
**实际时间**：0.5小时  
**状态**：✅ 已完成  
**完成日期**：2024-02-14  
**依赖**：任务 1.1, 1.2

**修改文件**：

- [x] `main/xiaozhi-server/core/handle/doorlock_intent_handler.py`

**实现内容**：

- [x] 导入 `ESP32CameraService`
- [x] 创建摄像头服务实例
- [x] 调用 `camera_service.capture_image()`：
  - [x] 传入 `device_id`
  - [x] 传入 `conn` 对象
  - [x] 设置 `question="访客到访拍照"`
  - [x] 设置 `timeout=10`
- [x] 处理返回结果（成功/失败）
- [x] 添加完整的日志记录
- [x] 添加异常处理

**验收标准**：

- [x] PIR触发后能成功拍照
- [x] 门铃触发后能成功拍照
- [x] 拍照失败时有明确日志
- [x] 超时后流程能正常继续
- [x] 返回的照片数据格式正确

**测试**：

- [ ] 单元测试：拍照方法调用
- [ ] 集成测试：PIR → 拍照流程
- [ ] 集成测试：门铃 → 拍照流程
- [ ] 集成测试：拍照超时处理

---

## 🎯 阶段二：统一人脸识别流程

### ✅ 任务 2.1：门铃事件触发人脸识别

**优先级**：P1（功能扩展）  
**预计时间**：2小时  
**实际时间**：1小时  
**状态**：✅ 已完成  
**完成日期**：2024-02-14  
**依赖**：任务 1.3

**修改文件**：

- [x] `main/xiaozhi-server/core/handle/textHandler/eventReportHandler.py`

**实现内容**：

- [x] 提取 `_trigger_face_recognition()` 统一方法：
  - [x] 参数：`conn`, `ts`, `param`, `trigger_type`
  - [x] 检查设备配置
  - [x] 检查是否启用意图识别
  - [x] 创建意图识别处理器
  - [x] 检查看护模式状态
  - [x] 调用 `handle_visitor()`
  - [x] 记录处理结果
- [x] 修改 `_handle_pir_event()` 调用统一方法
- [x] 修改 `_handle_bell_event()` 调用统一方法
- [x] 添加 `trigger_type` 日志标识

**验收标准**：

- [x] 门铃按下触发人脸识别
- [x] PIR触发人脸识别
- [x] 代码逻辑统一，无重复代码
- [x] 日志能区分触发类型（pir/bell）

**测试**：

- [ ] 单元测试：统一触发方法
- [ ] 集成测试：门铃 → 人脸识别
- [ ] 集成测试：PIR → 人脸识别

---

### ✅ 任务 2.2：实现开锁命令下发

**优先级**：P0（核心功能）  
**预计时间**：3小时  
**实际时间**：1小时  
**状态**：✅ 已完成  
**完成日期**：2024-02-14  
**依赖**：任务 1.3

**修改文件**：

- [x] `main/xiaozhi-server/core/handle/doorlock_intent_handler.py`

**实现内容**：

- [x] 添加 `_send_face_result()` 方法：
  - [x] 参数：`conn`, `result`, `user_id`, `access_granted`, `reason`
  - [x] 构建 `face_result` 消息（符合v5.2协议）
  - [x] 通过 WebSocket 发送
  - [x] 记录日志
- [x] 在 `handle_visitor()` 中调用：
  - [x] 识别成功有权限：`granted=True`
  - [x] 识别成功无权限：`granted=False`
  - [x] 识别失败或无权限：`granted=False`
- [x] 缓存识别结果到 `conn.last_face_result`

**验收标准**：

- [x] 识别成功有权限时下发 `face_result` (granted=true)
- [x] 识别失败时下发 `face_result` (granted=false)
- [x] 无权限时下发 `face_result` (granted=false, reason正确)
- [x] 消息格式符合v5.2协议（无seq_id）
- [x] 日志记录完整

**测试**：

- [ ] 单元测试：face_result消息构建
- [ ] 集成测试：识别成功 → 开锁
- [ ] 集成测试：识别失败 → 拒绝
- [ ] 集成测试：无权限 → 拒绝

---

## 🎯 阶段三：配置与开关

### ✅ 任务 3.1：添加人脸识别功能开关

**优先级**：P2（功能完善）  
**预计时间**：2小时  
**实际时间**：0.5小时  
**状态**：✅ 已完成  
**完成日期**：2024-02-14  
**依赖**：无

**修改文件**：

- [x] `main/xiaozhi-server/config/doorlock_config.yaml`
- [x] `main/xiaozhi-server/core/providers/doorlock/models.py`
- [x] `main/xiaozhi-server/core/handle/textHandler/eventReportHandler.py`

**实现内容**：

- [x] 在 `doorlock_config.yaml` 添加配置：
  ```yaml
  face_recognition:
    enabled: true
    max_retries: 3
    retry_interval: 1
    capture_timeout: 10
  ```
- [x] 在 `DoorlockConfig` 模型添加字段：
  - [x] `face_recognition_enabled: bool = True`
- [x] 在 `_trigger_face_recognition()` 添加检查：
  - [x] 检查 `config.face_recognition_enabled`
  - [x] 禁用时记录日志并返回
- [x] 添加配置加载日志

**验收标准**：

- [x] 配置项能正确加载
- [x] `enabled=false` 时不触发人脸识别
- [x] `enabled=true` 时正常触发
- [x] 配置参数生效（max_retries, timeout等）
- [x] 可通过API动态修改配置

**测试**：

- [ ] 单元测试：配置加载
- [ ] 集成测试：功能开关
- [ ] 集成测试：配置参数生效

---

## 📊 进度跟踪

### 完成情况统计

```
总任务数：6
已完成：6
进行中：0
未开始：0
完成率：100%
```

### 阶段完成情况

- [x] **阶段一**：拍照机制（3/3）✅
  - [x] 任务 1.1：MCP拍照同步等待
  - [x] 任务 1.2：HTTP服务器单例
  - [x] 任务 1.3：修复拍照方法

- [x] **阶段二**：人脸识别流程（2/2）✅
  - [x] 任务 2.1：门铃触发人脸识别
  - [x] 任务 2.2：实现开锁命令

- [x] **阶段三**：配置与开关（1/1）✅
  - [x] 任务 3.1：功能开关

---

## 🧪 测试清单

### 单元测试

- [ ] MCP工具调用测试
- [ ] Future超时处理测试
- [ ] 回调清理测试
- [ ] 单例模式测试
- [ ] 拍照方法调用测试
- [ ] face_result消息构建测试
- [ ] 配置加载测试

### 集成测试

- [ ] 完整拍照流程测试
- [ ] PIR → 拍照 → 人脸识别 → 开锁
- [ ] 门铃 → 拍照 → 人脸识别 → 开锁
- [ ] 拍照超时 → 降级处理
- [ ] 识别失败 → 意图识别对话
- [ ] 看护模式 → 监控和对话
- [ ] 功能开关测试
- [ ] 配置参数生效测试

### 性能测试

- [ ] 拍照响应时间测试
- [ ] 内存泄漏检查
- [ ] 并发多设备拍照测试
- [ ] 压力测试（连续触发）

---

## 📝 代码审查清单

### 代码质量

- [ ] 所有函数添加类型注解
- [ ] 所有函数添加中文文档字符串
- [ ] 使用 `async/await` 编写异步代码
- [ ] 使用 `loguru` 记录日志
- [ ] 无硬编码配置值
- [ ] 无 `print` 语句
- [ ] 异常处理完整
- [ ] 资源清理完整

### 日志规范

- [ ] 关键步骤有INFO日志
- [ ] 错误有ERROR日志和堆栈
- [ ] 调试信息有DEBUG日志
- [ ] 日志消息使用中文
- [ ] 日志包含关键上下文（device_id等）

### 安全性

- [ ] MCP客户端状态检查
- [ ] 工具可用性检查
- [ ] 超时保护
- [ ] 异常捕获
- [ ] 资源泄漏防护

---

## 🚀 部署清单

### 部署前检查

- [ ] 所有单元测试通过
- [ ] 所有集成测试通过
- [ ] 代码审查完成
- [ ] 文档更新完成
- [ ] CHANGELOG更新

### 部署步骤

1. [ ] 备份当前代码
2. [ ] 部署新代码
3. [ ] 重启服务
4. [ ] 验证服务启动
5. [ ] 验证MCP连接
6. [ ] 验证拍照功能
7. [ ] 验证人脸识别
8. [ ] 监控日志

### 回滚计划

- [ ] 准备回滚脚本
- [ ] 测试回滚流程
- [ ] 确认回滚触发条件

---

## 📚 文档清单

### 需要更新的文档

- [ ] `docs/my_docs/CHANGELOG.md` - 添加变更记录
- [ ] `docs/my_docs/esp32-vision-guide.md` - 更新拍照流程
- [ ] `docs/my_docs/smart-doorlock-ai-requirements.md` - 更新需求
- [ ] `README.md` - 更新功能说明（如需要）

### 需要创建的文档

- [ ] 人脸识别功能使用指南
- [ ] 故障排查指南
- [ ] API文档更新

---

## ⚠️ 风险与注意事项

### 技术风险

- [ ] **MCP客户端状态**：确保 `conn.mcp_client` 已初始化且就绪
- [ ] **工具可用性**：确保ESP32注册了 `capture_image` 工具
- [ ] **超时时间**：需要考虑拍照+上传的总时间（建议10-15秒）
- [ ] **并发安全**：多个设备同时拍照时的Future管理
- [ ] **HTTP服务器单例**：确保在服务器启动时正确初始化
- [ ] **内存泄漏**：确保Future和回调正确清理

### 业务风险

- [ ] **用户体验**：拍照失败时的降级策略
- [ ] **响应时间**：整个流程的响应时间要求
- [ ] **错误提示**：用户友好的错误提示
- [ ] **向后兼容**：不影响现有功能

### 缓解措施

- [ ] 完善的错误处理和日志
- [ ] 超时保护机制
- [ ] 资源清理机制
- [ ] 降级处理策略
- [ ] 充分的测试覆盖

---

## 📞 支持与联系

### 问题反馈

- 技术问题：提交 Issue
- 紧急问题：联系开发团队

### 相关文档

- 详细实施方案：`docs/my_docs/server-trigger-face-recognition-task-plan.md`
- MCP拍照指南：`docs/my_docs/esp32-vision-guide.md`
- 协议规范：`docs/my_docs/智能猫眼门锁系统-ESP32与服务器通信协议规范-v5.2.md`

---

## ✅ 最终验收标准

### 功能验收

- [ ] PIR触发能正常拍照并识别
- [ ] 门铃触发能正常拍照并识别
- [ ] 识别成功有权限能正常开锁
- [ ] 识别失败能进入意图识别对话
- [ ] 看护模式能同时监控和对话
- [ ] 功能开关能正常工作

### 性能验收

- [ ] 拍照响应时间 < 3秒
- [ ] 整体流程时间 < 10秒
- [ ] 无内存泄漏
- [ ] 并发10个设备正常工作

### 质量验收

- [ ] 单元测试覆盖率 > 80%
- [ ] 集成测试全部通过
- [ ] 代码审查通过
- [ ] 文档完整

---

**最后更新**：2024-02-14  
**文档版本**：v1.0  
**状态**：待实施
